services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile
    cap_add:
      - SYS_PTRACE
    security_opt:
      - seccomp=unconfined
    volumes:
      - ./:/workspace
    working_dir: /workspace
    tty: true
    stdin_open: true
